# อธิบายความเข้าใจ

## 1.การแบ่ง components ต่างๆ

### 1.1 แรกเริ่มตัวผมแยก Folder หลักแค่ 2 ส่วน คือ components และ pages เพื่อแบ่งว่าควรมี page และ component อะไรบ้าง

### 1.2 ในส่วนของ file App.jsx จะทำเป็นตัว router สำหรับแยก path ของ page home และ owner

### 1.3 แล้วก็สร้าง components เริ่มต้นด้วยแค่ navbar และ layout สำหรับแสดงปุ่มนำทางของ page และ layout ที่ไว้แสดงเนื้อหาในแต่ละ page

### 1.4 ตอนแรกทำ logic ทุกอย่างภายใน home page ก่อนเพื่อให้มันแสดงผลตามที่ต้องการให้ได้ก่อน

### 1.5 หลังจากได้ logic และการแสดงผลที่ต้องการแล้วก็ค่อยแยกเป็น components ต่างๆ และแยกส่วนเฉพาะของ logic ไว้อีกเป็น folder hooks

### 1.6 หลังจากแยก components ย่อยเสร็จจะได้ทั้งหมด คือ

#### 1.6.1 HomeTitle.jsx ที่เกี่ยวกับ Title และการเปลี่ยนชื่อ title section

#### 1.6.2 SectionButtons.jsx เป็นปุ่มที่เอาไว้เปลี่ยน section ระหว่าง user และ admin

#### 1.6.3 UserForm.jsx เป็นส่วนของ form ที่เอาไว้เพิ่มรายชื่อสมาชิก

#### 1.6.4 MemberTable.jsx เป็นส่วนที่เอาแสดงรายชื่อสมาชิกทั้งหมด

#### 1.6.5 และมีการทำ NotFoundPage.jsx เอาไว้เผื่อเกิดข้อผิดพลาด

#### 1.6.6 ในส่วนของ hooks หรือ useMembers.js ก็แยกเอา logic ออกมาโดยเฉพาะ

## 2.ตัวแปรต่างๆ state ต่างๆ

### 2.1 state:

#### 2.1.1 section, setSection สร้างขึ้นมาเพื่อเอาไว้เปลี่ยน Section ระหว่าง User กับ Admin ภายในหน้า Home

#### 2.1.2 members, setMembers สร้างขึ้นมาเพื่อรองรับสมาชิกจากการดึงข้อมูล Api มา

#### 2.1.3 formData, setFormData สร้างขึ้นมาเพื่อใช้ในการ set ข้อมูลสำหรับสร้างสมาชิกใหม่

### 2.2 function:

#### 2.2.1 fetchMembers ใช้สำหรับดึงข้อมูลของตัว api เข้ามา

#### 2.2.2 handleCreate ใช้สำหรับ handle ข้อมูลและเพิ่มสมาชิกเข้าไปใน members

#### 2.2.3 handleDelete ใช้สำหรับ handle ข้อมูลและลบออกจากข้อมูลสมาชิก

## 3.การจัดการ state

### 3.1 ในโปรเจคนี้จัดการ state ด้วยการ passing props ที่มาจากการแยก logic เป็นตัว hooks หรือก็คือ useMembers เพราะการส่ง props ในตัวโปรเจคนี้ไม่ได้ส่งลึกมากก็เลยเลือกใช้การส่ง props แทนการสร้าง Context มากกว่า และการแยก hooks ออกมามันทำให้โค้ดอ่านง่ายกว่าไปกองใน file Home.jsx

## 4. การใช้ useEffect ในโปรเจคนี้ใช้ตัว useEffect สำหรับรัน fetchMembers() ที่ดึงข้อมูล API มาและมีการใส่ Dependency Array ว่างไว้เพื่อให้แสดงผลครั้งเดียวป้องกันการเกิด loop ซ้ำซ้อน

## 5. การ fetch() ควรเป็นแบบ Asynchronous เพราะหลักๆแล้วการดึงข้อมูลมามันใช้เวลา และสามารถทำงานได้แบบไม่ต้องรอตัวแรกๆ เสร็จก่อน

## 6. อื่นๆ

### 6.1 การแยก components เพื่อที่จะให้อ่านโค้ดได้ง่ายขึ้นและโค้ดดูสะอาดตามากขึ้น

### 6.2 การที่เลือกเขียนใน components เดียวให้เสร็จก่อนเพื่อที่จะดูว่าเราสามารถแยกอะไรออกไปได้บ้าง เพราะเริ่มต้นเขียนไม่ได้คิดโครงสร้างเอาไว้ก่อน พอเขียนเบื้องต้นเสร็จก็เริ่มมองเห็นส่วนที่ควรแยกออกไป
